-- Script to list all rows of the 'first_table' from the 'hbtn_0c_0' database
-- Usage: mysql -u your_username -p your_password -e "USE hbtn_0c_0; SELECT * FROM first_table;"
-- This script does not specify the database name; it is provided as an argument when running the script
SELECT * FROM first_table;
